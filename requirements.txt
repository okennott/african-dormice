# Python dependencies for African Dormice analysis pipeline
# Install with: pip install -r requirements.txt
#
# Note: ete3 and cairosvg may require system-level libraries.
# See https://etetoolkit.org/new_download/ for platform-specific instructions.
# phyluce must be installed via Conda (see README.md).

# Core data science
pandas>=1.5.0
numpy>=1.23.0
openpyxl>=3.1.0        # Excel file I/O (used with pandas)

# Bioinformatics
biopython>=1.80        # SeqIO, AlignIO, Phylo parsing
ete3>=3.1.3            # Phylogenetic tree manipulation and visualization

# Configuration
pyyaml>=6.0            # YAML config file parsing

# Database (optional but recommended for morphology ETL)
duckdb>=0.9.0          # Embedded analytical database for taxonomy tables

# Visualization
cairosvg>=2.7.0        # SVG rendering (required for tree figure export)
